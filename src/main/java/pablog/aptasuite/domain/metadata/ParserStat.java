package pablog.aptasuite.domain.metadata;

public enum ParserStat {
    PROCESSED_READS,
    ACCEPTED_READS,
    CONTIG_ASSEMBLY_FAILS,
    INVALID_ALPHABET,
    FIVE_PRIME_ERROR,
    THREE_PRIME_ERROR,
    INVALID_CYCLE,
    TOTAL_PRIMER_OVERLAPS
}
