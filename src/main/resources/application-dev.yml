spring:
  datasource:
    # Persistent local database file
    url: jdbc:h2:file:./.data/selextrace;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update